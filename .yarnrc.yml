# React Native cannot run under Yarn PnP - it requires a real node_modules tree
# (Metro, the RN CLI and the CocoaPods/Gradle autolinking all walk node_modules).
nodeLinker: node-modules

# Resolve everything from the public npm registry.
npmRegistryServer: "https://registry.npmjs.org"
